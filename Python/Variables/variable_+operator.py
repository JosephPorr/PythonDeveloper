# The print() statement and combine text and variables using the + operato

var = "3.7.1"
print("Python version: " + var)

var = 100
var = 200 + 300
print(var)

# The var variable is created and assigned a value of 100. 
# Then, the same variable is assigned a new value: 
# the result of adding 200 to 300, which is 500.