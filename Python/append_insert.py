numbers = [111, 7, 2, 1]
print(len(numbers))  # prints the length of the range.
print(numbers)

###

numbers.append(4)  # THe append object is added to the end of the list.

print(len(numbers))
print(numbers)

###

numbers.insert(0, 222)  # The insert places the element anywhere in the list; zero is the location.
print(len(numbers))
print(numbers)

#  <--- If you use the hastag to seperate the code, like above, it needs to be concluded with a single #.