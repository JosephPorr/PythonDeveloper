# Python allows us to express such alternative plans. 
# This is done with a second, slightly more complex form of the conditional statement, 
# the if-else statement:

if true_or_false_condition:
    perform_if_condition_true
else:
    perform_if_condition_false

if the_weather_is_good:
    go_for_a_walk()
else:
    go_to_a_theater()
have_lunch()