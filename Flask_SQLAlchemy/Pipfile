[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
flask = "*"
psycopg2-binary = "*"
flask-sqlalchemy = "*"
flask-migrate = "*"
psycopg2 = "*"

[requires]
python_version = "3.8"

# Broken down into a pipcode before the interpreter.
